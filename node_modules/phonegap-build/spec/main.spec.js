/*!
 * Module dependencies.
 */

var PhoneGapBuild = require('../lib/phonegap-build'),
    phonegapbuild = require('../lib/main');

/*!
 * Specification: require('phonegap-build')
 */

describe('require("phonegap-build")', function() {
    //it('should be an instance of PhoneGapBuild', function() {
    //    expect(phonegapbuild).toEqual(jasmine.any(PhoneGapBuild));
    //});
});
